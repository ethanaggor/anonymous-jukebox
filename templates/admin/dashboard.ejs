<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset='utf-8'>
        <title>Dashboard page</title>
    </head>
    <body>
        <% if (isSessionInProgress) { %>
            Music Session in progress.
            <a href="/dashboard/stopSession">Stop session</a>
        <% } else { %>
            <a href="/dashboard/uploader">Upload from google drive/folder</a>
            <br>
            <a href="/dashboard/startSession">Start class session</a>
        <% } %>

    </body>
</html>